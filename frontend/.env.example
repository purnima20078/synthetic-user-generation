# Frontend Environment Configuration
# Leave empty in local development to route via Vite dev proxy (default: http://localhost:5000)
# Or specify full backend URL if running separated or in production
VITE_API_URL=
VITE_BACKEND_URL=http://localhost:5000
