rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Default catch-all allow read/write for seamless applet execution
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
